# Cài đặt

1. Clone repo

2. Cài dependencies:

```bash
pip install -r requirements.txt
```

3. Set API key:

```bash
export GOOGLE_API_KEY='your-api-key-here'
```

4. Chạy ứng dụng:

```bash
python main.py
```
